---
# ManageEngine Desktop Central Agent URL (required)
dc_agent_url: ""

# Agent username
dc_agent_username: "{{ ansible_user }}"

# Installation directory
dc_directory: "/usr/local/manageengine/uems_agent"

# Force installation
dc_force_install: true
